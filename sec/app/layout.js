export const metadata = {
  title: 'WhatsApp Bot - DOGMEN Pairing',
  description: 'Connect to WhatsApp Bot with pairing code DOGMEN',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  )
}
